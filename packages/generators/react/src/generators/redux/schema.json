{
  "$schema": "https://json-schema.org/schema",
  "$id": "OksaiReactReduxGenerator",
  "title": "Redux Generator",
  "description": "Add Redux Toolkit state management to a React project.",
  "cli": "nx",
  "type": "object",
  "properties": {
    "project": {
      "type": "string",
      "description": "The name of the project.",
      "alias": "p",
      "$default": {
        "$source": "projectName"
      },
      "x-prompt": "Which project would you like to add Redux to?"
    },
    "directory": {
      "type": "string",
      "description": "The directory to create the store in (relative to project src).",
      "default": "store"
    },
    "skipFormat": {
      "type": "boolean",
      "description": "Skip formatting.",
      "default": false
    },
    "skipPackageJson": {
      "type": "boolean",
      "description": "Skip adding dependencies.",
      "default": false
    }
  },
  "required": ["project"],
  "additionalProperties": false
}
